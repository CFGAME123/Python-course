"""
    escribir la siguiente expresion de forma algoritmica:
"""

a = float(input("escribe un numero: "))
b = float(input("escribe un numero: "))
c = float(input("escribe un numero: "))

resultado = (a **3 * (b**2 - 2*a*c))/(2*b)
print(resultado)